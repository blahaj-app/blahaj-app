{
  "name": "@blahaj-app/data",
  "private": true,
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc",
    "prepublishOnly": "pnpm run build"
  },
  "devDependencies": {
    "esbuild": "^0.17.0",
    "rimraf": "~4.0.0",
    "typescript": "~4.9.0"
  },
  "dependencies": {
  }
}